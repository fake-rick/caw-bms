import { HorizontalBox, VerticalBox, ListView, GroupBox } from "std-widgets.slint";

export component Log inherits Rectangle {
    background: #0000;
    VerticalBox {
        ListView {
        }
    }
} 